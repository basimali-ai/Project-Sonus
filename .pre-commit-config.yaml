repos:
  - repo: local
    hooks:
      - id: update-version
        name: Update version from latest Git tag
        entry: python scripts/update_version.py
        language: system
        always_run: true
        stages: [pre-commit]
